apply plugin: 'com.android.application'

android {
    namespace 'rahook.app'
    compileSdk 34
    defaultConfig {
        applicationId "rahook.app"
        minSdk 21
        targetSdk 34
        ndkVersion '25.1.8937393'
        ndk { abiFilters 'arm64-v8a', 'armeabi-v7a', 'x86', 'x86_64' }
        externalNativeBuild {
            cmake { cppFlags "-std=gnu11" }
        }
    }
    externalNativeBuild {
        cmake { path "CMakeLists.txt" }
    }
}
